import React, {Component} from 'react';

import {Col, Row} from "reactstrap";

export default class Login extends Component {
    render() {
        return (
            <Row>
                <Col>This da login.</Col>
            </Row>);
    }
}
